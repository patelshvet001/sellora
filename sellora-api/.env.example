PORT=4000
CLIENT_URL=http://localhost:3000

DATABASE_URL="mysql://root:@localhost:3307/sellora"

JWT_SECRET="wertyuidfvgbhnm456vbnm"

# === Gmail SMTP (default) ===
# Use a Gmail App Password, not your normal Gmail password.
# Create one at https://myaccount.google.com/apppasswords
GMAIL_USER="your@gmail.com"
GMAIL_APP_PASSWORD="kbbs uunt xmhe zxiz xxxx xxxx xxx"

# === Generic SMTP (alternative to Gmail) ===
# Uncomment and fill these in if you want to use a custom SMTP server instead of Gmail.
# When SMTP_HOST is set, the mailer will use these settings instead of Gmail.
# SMTP_HOST=smtp.yourdomain.com [sandbox.smtp.mailtrap.io]
# SMTP_PORT=587
# SMTP_USER=your-email@yourdomain.com
# SMTP_PASSWORD=your-email-password
# SMTP_SECURE=false
